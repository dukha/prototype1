<p class="pageheading"><%=t("headings.edit.heading", :model=>t("entities.course_type") )%></p>

<%= render 'form' %>

<%= link_to t('headings.new.heading', :model=> t('entities.course_type')), new_course_type_path %> | 
<%= link_to t("action.back"), course_types_path %>
