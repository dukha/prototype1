<%= semantic_form_for(@application_language) do |f| %>
  <%= f.error_messages %> 
  
	<% f.inputs do %>
	        <!-- <% @languages = Language.find(:all) %> -->
					<!-- <%Language.all.collect {|p| [ p.name, p.id ] }%> -->
				   <%= f.input :@iso_code,  :as => :select, :collection =>  Language.all.collect {|p| [ p.name, p.id ] }%>   
		
	<% end %>	
	<%= f.buttons do %>
      <%= f.commit_button :submit %>    
  <% end %>
	
	<% end %>

